# This file is kept for reference only.
# The project now uses uv. To install dependencies, run `uv sync` in backend/.
# To regenerate this file (e.g. for Docker/CI without uv): uv export --no-hashes > requirements.txt
